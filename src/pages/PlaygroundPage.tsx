import ExpandableText from "../components/ExpandableText";
import Onboarding from "../components/Onboarding";

const PlaygroundPage = () => {

  return <ExpandableText text="Testing the test involves testing. When testing text it is important that actions are taken to
  test text. When using testing frameworks to test text there maybe certain protocols in order to test the text. Here
  is an example of text that is able to expand and be testable. We will run this through a test and see if the expandable text
  is successful in the tests. I am writing this long paragraph in order to see if the testable text is able to succeed when it
  is being tested." />;
};

export default PlaygroundPage;
